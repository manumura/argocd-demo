apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  project: default
  source:
    chart: grafana
    helm:
      values: |
        pushgateway:
          enabled: false
    repoURL: https://charts.bitnami.com/bitnami
    targetRevision: 7.6.20
  syncPolicy:
    automated:
      prune: true
      selfHeal: true